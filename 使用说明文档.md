# 使用说明文档

本文档面向 Nebula Subscriptions 的日常使用者，按“启动服务、添加订阅、同步节点、接入客户端、创建智能代理、排查问题”的顺序说明。项目级架构、环境变量和 API 总览请参考 [README.md](README.md)。

## 1. 启动与登录

### 1.1 Docker Compose 启动

在项目根目录执行：

```bash
cp .env.example .env
docker compose up -d --build
```

默认地址：

| 服务 | 地址 |
| --- | --- |
| Web 管理后台 | `http://localhost:8088` |
| 后端 API 文档 | `http://localhost:8001/docs` |
| 后端健康检查 | `http://localhost:8001/health` |
| Redis / subconverter / Mihomo API | 默认不暴露到宿主机，仅 Docker 内网访问 |

默认管理员用户名：

```text
用户名：admin
```

管理员密码以 `.env` 中的 `ADMIN_PASSWORD` 为准。生产环境会拒绝默认弱密码和默认弱 Token，首次部署请先修改这些敏感配置：

```env
SECRET_KEY=please-change-this-secret
ADMIN_PASSWORD=your-strong-password
SUBSCRIPTION_TOKEN=your-subscription-token
```

Compose 默认传入 `TZ=Asia/Shanghai`，后端时间、定时任务和容器系统时区均按中国时区运行。

默认部署不会占用宿主机 `6379`、`25500`、`9090`；后端在 Docker 内网通过 `redis:6379`、`subconverter:25500`、`mihomo:9090` 调用依赖服务。本机调试依赖服务时，再叠加 `docker-compose.debug.yml` 暴露到 `127.0.0.1`。

### 1.2 生产环境更新

进入服务器上的项目目录后执行：

```bash
cd /opt/Nebula

cp .env .env.bak.$(date +%Y%m%d%H%M%S)
tar -czf nebula-data-bak-$(date +%Y%m%d%H%M%S).tar.gz backend/data

git pull --ff-only
docker compose config --quiet
docker compose up -d --build

docker compose ps
docker compose logs -f backend
```

如果确认只更新后端和前端镜像，可以执行：

```bash
docker compose up -d --build backend frontend
```

但当 Compose、端口映射、环境变量或 Mihomo runtime 相关配置发生变化时，建议完整执行 `docker compose up -d --build`。不要用新的 `.env.example` 直接覆盖生产 `.env`，只对照新增配置补齐。

### 1.3 页面导航

登录后左侧菜单包含：

| 页面 | 用途 |
| --- | --- |
| 首页 | 查看服务状态、订阅/节点/智能代理统计、流量统计和客户端订阅地址 |
| 订阅管理 | 管理订阅源、刷新订阅流量、维护订阅相关配置 |
| 节点管理 | 同步节点池、搜索节点、启用/停用节点、一键测速 |
| 智能代理 | 生成 HTTP/SOCKS5/Mixed 代理入口并应用到 Mihomo |
| 蚂蚁代理 | 登录 Ant 账号或上传 `ant.db`，把蚂蚁节点接入智能代理 |
| 规则管理 | 管理 ACL4SSR 等规则模板 |
| 配置模板 | 管理 Clash/Mihomo/sing-box/v2ray 输出模板 |
| 系统设置 | 管理公开访问地址、subconverter、Mihomo API 和 ACL4SSR 基础地址 |
| 日志中心 | 查看登录、订阅、节点、蚂蚁代理、智能代理和配置操作日志 |

### 1.4 移动端与 PWA

`v1.1.0` 起，Nebula 管理后台已适配手机和平板使用。移动端会固定顶部栏，底部导航支持横向滑动，列表页会切换为卡片布局，主要操作按钮、分页、诊断弹窗、节点选择弹窗和二次确认弹窗均按触控场景优化。

Nebula 同时支持安装为 PWA 应用：

- Android、桌面 Chrome/Edge 等浏览器在满足安装条件时，会在顶部显示安装按钮，也可使用浏览器菜单中的安装入口。
- iPhone / iPad 请使用 Safari 打开管理后台，通过「分享」菜单选择「添加到主屏幕」。
- PWA 安装通常要求 HTTPS；本机 `localhost` 调试属于浏览器允许的例外。局域网 HTTP 访问时，部分浏览器可能不会开放安装入口。
- 离线能力只覆盖管理后台应用壳、图标和离线提示页。订阅、节点、日志、智能代理状态、健康检测和配置保存仍需要连接后端服务。
- 发布新版本后，前端检测到 Service Worker 更新会提示刷新；如果图标或页面没有立即更新，可关闭所有 Nebula 页面后重新打开，或清理站点缓存。

## 2. 推荐首次使用流程

1. 启动 Docker Compose。
2. 登录 Web 管理后台。
3. 进入「订阅管理」，新增一个订阅，分组填 `default`。
4. 进入「节点管理」，点击「同步节点池」，确认节点出现。
5. 回到「首页」，复制 `/api/sub/mihomo` 地址导入 Mihomo/Clash 客户端。
6. 如需给浏览器、系统代理、OpenWRT 或 Proxifier 提供固定代理入口，进入「智能代理」新增代理并点击「重新应用到 Mihomo」。
7. 如需使用蚂蚁代理节点，先进入「蚂蚁代理」完成账号登录或上传 `ant.db`，再到「智能代理」新增代理并选择数据来源「蚂蚁节点」。

## 3. 订阅管理

进入「订阅管理」后点击「新增订阅」。

| 字段 | 说明 | 示例 |
| --- | --- | --- |
| 名称 | 订阅来源名称，会用于客户端节点名前缀 | `机场 A` |
| URL | 原始订阅地址，只支持 `http`/`https` | `https://example.com/api/v1/client/subscribe?token=xxx` |
| 分组 | 客户端接口可用 `group=` 只输出该分组 | `default`、`work`、`media` |
| 标签 | 写入节点标签，便于智能代理按标签筛选 | `streaming`、`香港`、`备用` |
| 优先级 | 数字越小越靠前 | `10` |
| 更新间隔秒 | 字段保留在订阅信息中，最小值 `60` | `3600` |
| 启用 | 是否参与节点池和客户端输出 | 开启 |
| 备注 | 自定义说明 | `主力订阅` |

保存后，Nebula 会把订阅状态标记为 `syncing`，并在后台触发一次节点池同步。新增、修改、启用/停用、删除订阅都会触发后台同步。

订阅 URL 默认有安全限制：

- 只允许 `http` 和 `https`。
- 默认阻止私网、回环、链路本地、保留和组播地址。
- 配置 `URL_ALLOWLIST=example.com,sub.example.org` 后，只允许这些域名及其子域名。
- 如确实需要内网订阅，可设置 `URL_BLOCK_PRIVATE_NETWORKS=false`。

### 3.1 状态和流量

订阅列表会展示：

| 信息 | 说明 |
| --- | --- |
| 状态 | 启用或停用 |
| 更新状态 | `syncing`、`ok`、`failed`、`disabled` 等后端同步状态 |
| 最近更新时间 | 最近一次同步或状态变更时间 |
| 流量 | 上传、下载、已用、总量、剩余和到期时间 |

流量来自订阅响应头：

```text
subscription-userinfo: upload=...; download=...; total=...; expire=...
```

点击「刷新流量」会立即采集启用订阅的流量并写入快照。某个订阅采集失败时，页面会显示错误；如果之前有可用快照，会保留上一次流量数字并标记为过期数据。

### 3.2 订阅配置

点击「订阅配置」可维护：

| 配置项 | 说明 |
| --- | --- |
| `subscription_token` | 公开订阅接口使用的 Token |
| `cache_ttl_seconds` | subconverter 输出缓存有效期，单位秒 |
| `traffic_poll_interval_minutes` | 后台流量轮询间隔，单位分钟；`0` 表示关闭 |

后台任务每分钟检查一次设置，到达间隔后才会真正执行流量轮询。

## 4. 节点管理

节点池是 Clash/Mihomo/v2rayN 输出和智能代理的核心数据来源。

同步节点池时，Nebula 会：

1. 读取启用订阅，并按优先级排序。
2. 对每个订阅单独调用 `subconverter` 转为 Clash YAML。
3. 解析 YAML 中的 `proxies`。
4. 使用 `node_filter_patterns` 过滤名称命中的伪节点。
5. 按 `server + port + uuid/password/cipher/psk` 去重。
6. 识别国家/地区并写入标签。
7. 全部目标订阅成功后，替换该范围内旧节点。
8. 如果有订阅失败，保留旧节点池并记录错误。

节点列表支持按关键词、国家/地区、分组和启用状态筛选。可单独启用或停用节点，停用后的节点不会进入客户端输出和智能代理候选池。

### 4.1 节点命名

客户端输出时会重新生成更清晰的节点名：

```text
【机场 A】香港01 ...
【机场 A】日本01 ...
【机场 B】美国01 ...
```

`【机场 A】` 来自订阅名称，后面的国家/序号来自自动识别和同源计数。

### 4.2 节点池配置

点击「节点池配置」可维护：

| 配置项 | 说明 |
| --- | --- |
| `node_filter_patterns` | 节点名称过滤通配符，多个规则用英文逗号分隔；保存后会自动触发一次节点池同步 |

过滤示例：

```text
*天*,*剩余*,*到期*,*重置*
```

如果订阅源里有“剩余 23 天”“套餐到期”等提示项，配置后会自动重新同步并跳过这些伪节点。节点池同步以事件触发为主：新增、修改、启用/停用、删除订阅会触发后台同步；订阅到达自身更新间隔时会刷新该订阅并同步对应节点；节点列表为空时也会自动补齐。需要立即全量刷新时，可点击「同步节点池」。

### 4.3 一键测速

点击「一键测速」会并发尝试连接节点的 `server:port`，并把 TCP 连接耗时写入「延迟」列。

说明：

- 默认只测速已启用节点。
- 如果设置了分组筛选，只测速该分组内节点。
- 超时范围由后端限制在 `300ms` 到 `15000ms`。
- 这是入口 TCP 连通性测试，不等同于完整代理链路测速或解锁能力测试。

## 5. 首页 Dashboard

首页展示：

| 区域 | 内容 |
| --- | --- |
| 运行状态 | 订阅数、启用订阅数、节点数、智能代理数、缓存键数 |
| 服务状态 | Redis、subconverter、Mihomo 可用性和 Mihomo 版本 |
| 流量统计 | 总流量、已用、剩余、最近到期、流量采集时间 |
| 客户端订阅地址 | Mihomo/Clash.Meta、Clash、sing-box、v2rayN 地址 |
| 智能代理 | 已创建代理的端点地址，可直接复制 |

首页会通过 WebSocket 周期刷新 Dashboard 和智能代理状态。复制订阅地址时，如果「系统设置」中配置了「订阅公开访问地址」，会优先使用该地址；否则按当前页面访问地址生成完整 URL。

如果你从 `http://localhost:5173` 访问开发前端，复制出的地址也会是 `localhost:5173`，适合浏览器调试。正式客户端通常使用 `http://localhost:8088` 或后端直连 `http://localhost:8001`。

智能代理地址会优先使用「代理公开访问地址」。只填写域名时会保留每个代理自己的端口；填写带端口的完整地址时，页面展示会统一使用该公开端口。

## 6. 客户端订阅接入

公开订阅接口格式：

```text
/api/sub/{target}?token=<subscription_token>&group=default&emoji=true
```

支持的 `target`：

| 客户端/格式 | 接口 |
| --- | --- |
| Mihomo / Clash.Meta | `/api/sub/mihomo` |
| Clash | `/api/sub/clash` |
| sing-box | `/api/sub/singbox` |
| v2rayN / v2ray | `/api/sub/v2ray` |

通用参数：

| 参数 | 必填 | 说明 |
| --- | --- | --- |
| `token` | 是 | 公开订阅 Token |
| `group` | 否 | 只输出指定分组 |
| `template` | 否 | 指定配置模板名称 |
| `emoji` | 否 | 是否启用 Emoji，默认 `true` |

### 6.1 Mihomo / Clash.Meta

推荐给 Mihomo、Clash Verge、Clash Meta for Android：

```text
http://localhost:8088/api/sub/mihomo?token=nebula-sub-token&group=default&emoji=true
```

该接口从节点池生成 YAML，内置代理组包括：

| 代理组 | 说明 |
| --- | --- |
| `🚀 节点选择` | 主选择组，包含自动、故障转移、负载均衡、地区组、流媒体、ChatGPT 和全部节点 |
| `♻️ 自动选择` | `url-test` 自动测速 |
| `🛟 故障转移` | `fallback` 故障切换 |
| `⚖️ 负载均衡` | `load-balance` 一致性哈希 |
| `🇭🇰 香港节点`、`🇯🇵 日本节点`、`🇺🇸 美国节点` | 按识别国家/地区分组，无匹配时回退为全部节点 |
| `🎬 流媒体` | 流媒体规则使用 |
| `🤖 ChatGPT` | OpenAI/ChatGPT 规则使用 |
| `🐟 漏网之鱼` | 默认兜底组 |

### 6.2 Clash

```text
http://localhost:8088/api/sub/clash?token=nebula-sub-token&group=default&emoji=true
```

当前 Clash 和 Mihomo 输出都来自同一个节点池生成器，差异主要由客户端兼容性和模板内容决定。

### 6.3 sing-box

```text
http://localhost:8088/api/sub/singbox?token=nebula-sub-token&group=default&emoji=true
```

sing-box 当前由 `subconverter` 直接聚合转换启用订阅，不从节点池生成原生 sing-box 配置。

### 6.4 v2rayN / v2ray

```text
http://localhost:8088/api/sub/v2ray?token=nebula-sub-token&group=default&emoji=true
```

v2rayN 请使用 `/api/sub/v2ray`。如果导入 `/api/sub/mihomo` 或 `/api/sub/clash`，通常只会出现一个 `Custom` 配置项，不能展开为节点列表。

当前 v2rayN 输出支持：

```text
vmess://
vless://
trojan://
ss://
hysteria2://
```

如果节点池中没有这些兼容协议，会返回“没有 v2rayN 兼容节点”的错误。

### 6.5 指定模板

如果创建了名为 `Streaming` 的模板：

```text
http://localhost:8088/api/sub/mihomo?token=nebula-sub-token&group=default&template=Streaming&emoji=true
```

## 7. 规则管理和配置模板

### 7.1 规则管理

进入「规则管理」可维护规则模板：

| 字段 | 说明 |
| --- | --- |
| 名称 | 模板名称 |
| 描述 | 可选说明 |
| 远程配置 URL | ACL4SSR 等远程配置地址 |
| YAML | 自定义 YAML 内容 |
| 默认 | 设为默认后会取消其他规则模板默认状态 |

系统首次启动会初始化：

```text
ACL4SSR Online
```

默认远程地址来自 `ACL4SSR_CONFIG_URL`。

### 7.2 配置模板

进入「配置模板」可维护客户端配置模板：

| 字段 | 说明 |
| --- | --- |
| 名称 | 模板名称，订阅接口可用 `template=` 指定 |
| 目标 | `clash`、`clashmeta`、`singbox`、`v2ray` 等目标标识 |
| 配置 URL | 传给 `subconverter` 的 `config=` 参数，主要用于 sing-box 等转换链路 |
| YAML | Clash/Mihomo 节点池生成器会读取其中的基础字段 |
| 默认 | 同一目标只能有一个默认模板 |

Clash/Mihomo 生成器会读取模板 YAML 中的：

```yaml
mixed-port: 7890
allow-lan: true
mode: rule
log-level: info
external-controller: 127.0.0.1:9090
dns: {}
rule-providers: {}
rules: []
```

如果模板没有提供 `dns`、`rule-providers` 或 `rules`，Nebula 会使用内置默认值。

## 8. 蚂蚁代理

「蚂蚁代理」页面用于把 Ant 客户端里的节点接入 Nebula。它不再在本机额外暴露一个独立桥接代理给用户使用，而是把 Ant 节点作为「智能代理」的数据来源，由 Mihomo 统一生成对外代理入口和调度策略。

### 8.1 登录方式

蚂蚁代理支持两种数据来源：

| 方式 | 说明 |
| --- | --- |
| 账号登录 | 在 Nebula 中输入 Ant 账号、密码和客户端版本，由后端调用 Ant 接口登录并拉取节点 |
| `ant.db` 登录 | 上传 Ant 客户端导出的 `ant.db` 或兼容 msgpack 文件，由后端解析其中的节点数据 |

当前客户端版本下拉默认只有 `2.0.9`。后续如果 Ant 客户端接口需要不同版本号，可在页面版本选项扩展后切换。

登录或上传成功后，Nebula 会把可持久化数据保存到数据库中，后端重启后会自动恢复。这里保存的是 Ant 数据、来源类型、账号标识、客户端版本、节点数据和相关运行配置；不会依赖用户本机的 `C:\Users\...\ant.db` 路径。页面上的在线人数、连接数、延迟等动态信息属于运行态数据，会按页面刷新、测速或后台任务更新。

### 8.2 页面展示和缓存

蚂蚁代理页面会按线路拆分节点：

| 线路 | 说明 |
| --- | --- |
| 免费专线 | Ant 返回的免费线路节点 |
| 付费专线 | Ant 返回的付费线路节点 |

页面首次打开时会判断是否已有登录/上传数据；同一浏览器标签页内，从其他页面切回蚂蚁代理时会优先使用浏览器 `sessionStorage` 缓存，避免反复请求和从登录页闪到主体页面。关闭浏览器标签页后，下一次打开会重新读取后端状态。

节点列表中的「在线」和「延迟」来自 Ant 接口或 Nebula 测速结果：

- 点击「刷新」：账号登录方式会重新向 Ant 拉取节点和在线等数据；`ant.db` 登录方式不会刷新，需要重新上传文件。
- 点击「测速」：Nebula 会对当前线路节点执行 Ant 节点 TCP/TLS/WS 握手测速，并把结果写入当前页面数据；该测速不依赖 Docker 容器内的 `ping` 或 ICMP 权限。
- 切换免费/付费线路：优先读取当前标签页缓存，未缓存过时再向后端加载。

页面顶部「转发流量」统计所有经过 Ant 内部适配器的上行/下行字节，包括智能代理真实访问和 Mihomo 健康检测产生的流量。该统计会按智能代理核心流量的方式采样并持久化到数据库；后端重启后会从数据库恢复累计值，后台维护任务每分钟自动写入一次，页面打开时也会轻量轮询动态状态。

### 8.3 定时刷新

点击蚂蚁代理页顶部「定时刷新」可配置后台任务：

| 配置项 | 说明 |
| --- | --- |
| 自动刷新 | 是否启用账号登录节点的后台刷新 |
| 刷新间隔 | 单位小时，默认 `6` 小时 |

后台调度器每分钟检查一次配置，只有满足这些条件才会执行：

1. 已启用自动刷新。
2. 当前来源是账号登录。
3. 已到达配置的刷新间隔。
4. 后端已恢复到有效的 Ant 登录数据。

`ant.db` 登录不会被后台任务刷新，因为文件内容只有用户重新上传时才会变化。定时刷新成功后会保存 Ant 状态；如果智能代理引用了蚂蚁节点，Nebula 会按需重新生成并热重载 Mihomo runtime。刷新失败时会记录日志，并按配置间隔再次尝试，避免每分钟重复报错。

相关默认环境变量：

```env
ANT_PROXY_AUTO_REFRESH_ENABLED=true
ANT_PROXY_AUTO_REFRESH_INTERVAL_MINUTES=360
```

### 8.4 和节点管理的关系

蚂蚁节点不会写入「节点管理」的普通节点池。两类节点来源的边界如下：

| 来源 | 存储位置 | 用途 |
| --- | --- | --- |
| 订阅节点 | 节点管理页面和 `nodes` 表 | 客户端订阅输出、智能代理订阅节点来源 |
| 蚂蚁节点 | 蚂蚁代理服务状态和持久化数据 | 仅作为智能代理的「蚂蚁节点」数据来源 |

这样做是为了避免把 Ant 私有节点混入通用订阅输出，同时仍然能让它们参与智能代理的统一调度、健康检测和端口管理。

### 8.5 接入智能代理

在「智能代理」新增代理时，将「数据来源」选择为「蚂蚁节点」后，可配置：

| 配置 | 说明 |
| --- | --- |
| 节点来源 | `所有蚂蚁节点` 或 `指定节点` |
| 指定地区 | 不选则不限地区 |
| 指定线路/来源 | 可选 `免费专线`、`付费专线`；不选则不限线路 |
| 协议类型 | 按 Ant 节点传输类型筛选，如 `ws`、`wss`、`tcp` |
| 调度策略 | 继续使用智能代理里的 `stable`、`fallback`、`url-test`、`load-balance` 等策略 |

蚂蚁代理页本身不配置对外代理端口。对外可用的 HTTP/SOCKS5/Mixed 地址只在「智能代理」中创建，并使用 `SMART_PROXY_PORT_START` / `SMART_PROXY_PORT_END` 配置的端口池，默认 `37890-37900`。

### 8.6 运行机制

当某个启用的智能代理选择了「蚂蚁节点」作为数据来源时：

1. Nebula 根据智能代理配置筛选 Ant 候选节点。
2. 后端为候选 Ant 节点启动内部 SOCKS 适配器。
3. Nebula 生成 Mihomo runtime YAML，把这些内部 SOCKS 适配器写为 Mihomo 上游节点。
4. Mihomo 按智能代理的策略组进行调度，并通过 listener 对外提供代理端口。

因此，用户侧只需要连接智能代理展示的地址；不需要手动连接 Ant 内部适配器，也不需要在服务器上启动本地 Ant 客户端桥接。

Docker Compose 部署时，后端容器默认用 `ANT_ADAPTER_BIND_HOST=0.0.0.0` 启动内部适配器，Mihomo 容器默认通过 `ANT_ADAPTER_CONNECT_HOST=backend` 连接这些内部上游。当连接地址是 `backend` 这类 Docker 服务名时，Nebula 会给 Mihomo runtime 写入 `dns.proxy-server-nameserver: [127.0.0.11]`，让 Mihomo 解析服务名时走 Docker 内置 DNS。本地开发如果使用 `host.docker.internal` 或 Docker Desktop 网关 IP，则不会默认注入该 DNS。自定义网络、外置 Mihomo 或跨主机部署时，需要把 `ANT_ADAPTER_CONNECT_HOST` 改成 Mihomo 能访问到后端容器的地址，并按需配置 `MIHOMO_PROXY_SERVER_NAMESERVERS`。

后端重启或重建后，内存中的 Ant 内部适配器端口会重新分配。Nebula 启动时会检测已启用的蚂蚁智能代理并自动重新应用 Mihomo runtime；手动点击智能代理「诊断」时，也会先重建 Ant 内部适配器并重新应用 runtime，再执行策略组测速和 listener 自检。

## 9. 智能代理

智能代理用于创建可直接使用的代理入口，例如：

```text
http://user:pass@127.0.0.1:37890
socks5://user:pass@127.0.0.1:30002
http://token:your-token@127.0.0.1:30003
```

Nebula 不自行实现代理协议栈，而是生成 Mihomo runtime 配置：

```text
backend/data/mihomo-runtime.yaml
```

点击「重新应用到 Mihomo」后，Nebula 会生成全部启用智能代理的 runtime YAML，并调用 Mihomo `/configs?force=true` 热重载。

### 9.1 全局配置

点击「全局配置」可查看部署端口范围，并维护：

| 配置项 | 说明 |
| --- | --- |
| 部署端口范围 | 只读展示，由 `SMART_PROXY_PORT_START` / `SMART_PROXY_PORT_END` 和 Docker 端口映射共同决定，默认 `37890-37900` |
| `SMART_PROXY_BIND_HOST` | Docker 宿主机代理端口绑定地址，只读展示；默认 `127.0.0.1`，外部设备访问需改为 `0.0.0.0` |
| `smart_proxy_auto_apply_interval_minutes` | 定时检查 runtime 内容并仅在变化时重新应用到 Mihomo，`0` 表示关闭 |
| `smart_proxy_monitor_interval_minutes` | 后台运行状态监控和访问检查频率，`0` 表示关闭 |
| `mihomo_runtime_config_path` | Nebula 写入 runtime YAML 的路径 |
| `mihomo_core_config_path` | Mihomo 进程/容器内看到的同一 runtime YAML 路径 |
| `mihomo_proxy_server_nameservers` | Mihomo 解析上游代理服务器名使用的 DNS；Docker Compose 默认由 `MIHOMO_PROXY_SERVER_NAMESERVERS=127.0.0.11` 写入 |
| `traffic_guard_enabled` | 是否启用流量保护 |
| `min_remaining_mb` | 剩余流量小于等于该值时排除节点 |
| `low_remaining_mb` | 剩余流量小于等于该值时降权排序 |
| `expire_soon_days` | 订阅将在该天数内到期时降权排序 |
| `exclude_unknown_traffic` | 是否排除没有 `subscription-userinfo` 的订阅节点 |

日常编辑代理时建议保持 `smart_proxy_auto_apply_interval_minutes=0`，手动确认后再点击「重新应用到 Mihomo」。开启后，后台只会在 runtime 内容变化或重载失败需要记录时写入审计并热重载，避免无变化时反复打断连接。

### 9.2 新增代理

点击「新增代理」后可选择三种方式：

| 方式 | 说明 |
| --- | --- |
| 快速模板 | 使用香港稳定线路、日本 ChatGPT 专线、美国 ChatGPT 专线、流媒体线路、多节点负载均衡等预设 |
| 高级编排 | 自行配置代理类型、策略、场景、节点来源、认证和健康检测 |
| 手动选节点 | 从当前数据来源搜索并按顺序勾选具体节点 |

基础字段：

| 字段 | 说明 |
| --- | --- |
| 名称 | 代理名称 |
| 代理类型 | `http`、`socks`、`mixed`；`https` 会归一为 `http` |
| 监听地址 | 如 `127.0.0.1`、`0.0.0.0` |
| 端口 | 留空时自动从部署端口范围内分配 |
| 策略 | `stable`、`fallback`、`url-test`、`load-balance`、`round-robin`、`select`、`relay` |
| 场景 | `general`、`latency`、`ai`、`streaming` 等 |
| 数据来源 | `订阅节点` 或 `蚂蚁节点` |
| 健康检测 URL | Mihomo 策略组测速 URL |
| 检测间隔 | 写入 Mihomo group 的 `interval` |
| 延迟容差 | `url-test` 策略的 `tolerance` |
| 用户名/密码 | 写入 Mihomo listener 的 `users` |
| 访问 Token | 写入为用户名 `token`、密码为该 Token 的用户 |
| IP 白名单 | 支持单 IP、CIDR 和 `*`；留空表示不限制 |

选择「混合代理」时，Mihomo 会在同一个监听端口上同时接受 HTTP(S) 和 SOCKS5 连接。界面会展示两条地址，例如：

```text
http://user:pass@example.com:37890
socks5://user:pass@example.com:37890
```

这两条地址共用同一个端口，不会额外占用第二个代理端口。

订阅节点来源：

| 模式 | 说明 |
| --- | --- |
| 全部节点 | 使用所有已启用节点 |
| 指定订阅 | 只使用选中订阅来源的节点 |
| 指定国家/地区 | 只使用识别出的国家/地区节点 |
| 指定标签 | 节点必须包含所选标签 |
| 手动节点 | 只使用手动勾选的节点，并保留选择顺序 |

蚂蚁节点来源：

| 模式 | 说明 |
| --- | --- |
| 所有蚂蚁节点 | 使用蚂蚁代理页已登录或已上传数据中的全部 Ant 节点 |
| 指定节点 | 从 Ant 节点中手动勾选候选节点 |

选择「蚂蚁节点」时，指定地区、指定线路/来源和协议类型会作为额外过滤条件一直可用。指定线路/来源当前用于选择 `免费专线` 或 `付费专线`；不选择则不限线路。

协议类型过滤只对非手动节点模式生效。策略节点可以在候选节点范围内进一步限制或排序；`relay` 和 `round-robin` 至少需要 2 个候选/策略节点。

### 9.3 策略说明

| 策略 | 适用场景 |
| --- | --- |
| `stable` | 账号登录、游戏、风控敏感应用；当前节点仍可用时优先保持 |
| `fallback` | 主备顺序固定，故障时切换 |
| `url-test` | 低延迟优先，按检测 URL 选择更快节点 |
| `load-balance` | 多设备或批量请求，一致性哈希分流 |
| `round-robin` | 多节点轮询，适合平均使用多个出口 |
| `select` | 在 Mihomo 策略组中手动选择当前节点 |
| `relay` | 链式代理，高级场景，按策略节点顺序串联 |

### 9.4 应用与状态

智能代理页面顶部按钮：

| 操作 | 说明 |
| --- | --- |
| 「重新应用到 Mihomo」 | 生成 runtime YAML 并热重载 Mihomo |
| 「预览配置」 | 生成并查看 runtime 内容，但不热重载 Mihomo |
| 「访问检查」 | 立即巡检连接来源 IP，关闭不符合白名单的连接 |
| 「全局配置」 | 查看部署端口范围，修改 runtime 路径、定时检查和流量保护策略 |

每个代理行可执行：

- 查看运行状态和健康检测。
- 启动代理：启用并自动应用到 Mihomo。
- 停止代理：停用并自动应用到 Mihomo。
- 编辑代理：保存后可选择是否立即应用。
- 删除代理：删除并自动应用到 Mihomo。

刷新订阅流量时，如果某个被智能代理引用的订阅从可用变为到期/流量耗尽，系统会对该订阅下的相关节点做一次 TCP 可用性检测；节点不可用时会自动重新应用 Mihomo runtime，让代理切走。订阅恢复且节点检测可用时，也会自动重新应用 runtime，把节点加回候选池。自动重载有 5 分钟冷却时间，冷却期内的变化会合并处理；重载前还会比较 runtime 内容，没有实际变化时不会热重载。

代理状态含义：

| 状态 | 说明 |
| --- | --- |
| `running` | Mihomo 中已找到策略组和 listener |
| `configured` | runtime 已生成，但页面还未确认运行态 |
| `degraded` | 候选节点为空、策略组缺失或 Mihomo API 异常 |
| `core_unavailable` | 无法连接 Mihomo API |
| `stopped` | 代理未启用，不会写入 listener |

### 9.5 健康检测和切换日志

健康检测会：

1. 调用 Mihomo 策略组测速接口。
2. 通过 Mihomo listener 端口自检一次实际代理入口。
3. 检查 Docker 宿主机代理端口绑定地址是否只允许本机访问。
4. 更新候选节点延迟。
5. 统计在线节点、失败节点、平均延迟、最优节点和当前节点。
6. 写入健康检测日志。
7. 对 `ai` 场景额外检测 `https://chat.openai.com/cdn-cgi/trace`。
8. 对 `streaming` 场景额外检测 Netflix URL。

切换日志记录当前节点变化，稳定优先策略会在检测或状态刷新发现切换后重新应用 runtime，让当前节点在候选列表中优先。

### 9.6 访问控制

访问控制分三层：

- 用户名/密码：由 Mihomo listener 原生校验。
- 访问 Token：Nebula 写入为 `token:<token>` 用户，客户端按标准代理认证填写。
- IP 白名单：Nebula 通过 Mihomo `/connections` 巡检连接来源 IP，发现不在白名单内的连接会调用 Mihomo API 关闭。

IP 白名单支持：

```text
127.0.0.1
192.168.1.0/24
*
```

留空表示不限制来源 IP。

## 10. 系统设置

系统设置用于维护运行后的业务配置，当前包含：

| 配置项 | 说明 |
| --- | --- |
| 订阅公开访问地址 | 用于生成首页和 WebSocket 推送里的客户端订阅地址；留空时使用当前访问地址 |
| 代理公开访问地址 | 用于生成首页和智能代理页面的代理地址；只填域名时保留代理原端口，填写端口时统一使用该公开端口 |
| subconverter 服务地址 | 生产环境可修改；保存前会检测新地址是否可用 |
| Mihomo API 地址 | 生产环境可修改；保存前会请求 `/version` 检测是否可用 |
| Mihomo API 密钥 | Mihomo External Controller 的 Bearer Token，未配置密钥时可留空 |
| ACL4SSR 远程规则地址 | 默认规则模板和转换链路使用的远程规则配置 |

Redis 连接地址不会在页面展示，也不能通过系统设置修改；它只在部署时通过 `REDIS_URL` 配置。

`subconverter 服务地址` 保存成功后，Nebula 会清理订阅转换缓存，并触发一次节点池同步。`Mihomo API 地址` 或 `Mihomo API 密钥` 保存成功后，Nebula 会立即刷新智能代理运行状态；后续重新应用 runtime、健康检测和访问控制都会使用新的 Mihomo API 配置。

如果修改后的 subconverter 或 Mihomo API 不可用，保存会失败并显示错误，旧配置会继续保留。

本地开发环境中，也就是 `APP_ENV=development` 时，`subconverter 服务地址`、`Mihomo API 地址`、`Mihomo API 密钥` 由 `backend/.env.development` 或环境变量控制，系统设置页面会只读展示，不能保存修改。

## 11. 日志中心

日志中心可按类型和时间筛选审计日志。常见类型包括：

| 类型 | 记录内容 |
| --- | --- |
| 认证 | 登录 |
| 订阅 | 新增、修改、删除、刷新、刷新失败、流量刷新 |
| 节点池 | 手动/后台/订阅刷新触发的同步结果 |
| 节点 | 启用、停用、一键测速 |
| 蚂蚁代理 | 定时刷新、刷新失败、后台刷新配置变更 |
| 智能代理 | 新增、修改、删除、启动、停止、重新加载、健康检测、访问控制 |
| 系统配置 | 设置项变更 |
| 规则模板 | 规则模板 CRUD |
| 配置模板 | 配置模板 CRUD |

同步或检测类日志会包含数量统计和前几条错误，适合定位订阅或 Mihomo 问题。

## 12. 本地开发说明

本地开发配置不会提交到仓库。可以先复制示例文件：

```powershell
Copy-Item backend\.env.development.example backend\.env.development
```

[backend/.env.development.example](D:/AppData/Nebula/backend/.env.development.example:1) 默认适配宿主机后端通过 `127.0.0.1` 调用 Compose 依赖服务的场景：

```env
APP_ENV=development
TZ=Asia/Shanghai
REDIS_URL=redis://127.0.0.1:6379/0
SUBCONVERTER_URL=http://127.0.0.1:25500
MIHOMO_API_URL=http://127.0.0.1:9090
MIHOMO_RUNTIME_CONFIG_PATH=./data/mihomo-runtime.yaml
MIHOMO_CORE_CONFIG_PATH=/root/.config/mihomo/mihomo-runtime.yaml
```

如果你使用的 Redis 设置了密码，请把 `backend/.env.development` 改成：

```env
REDIS_URL=redis://:your-password@127.0.0.1:6379/0
```

启动依赖：

```bash
docker compose -f docker-compose.yml -f docker-compose.debug.yml up -d redis subconverter mihomo
```

默认 `docker compose up -d` 不暴露 Redis、subconverter、Mihomo API 到宿主机；只有叠加 `docker-compose.debug.yml` 后，宿主机后端才可以访问 `127.0.0.1:6379`、`127.0.0.1:25500`、`127.0.0.1:9090`。

启动后端：

```powershell
cd backend
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```

启动前端：

```powershell
cd frontend
npm install
npm run dev
```

开发地址：

```text
前端：http://localhost:5173
后端：http://localhost:8001
```

Vite 已代理 `/api` 和 `/health` 到后端，WebSocket 状态推送也会走同一代理。

本地开发环境下，系统设置里的 subconverter 和 Mihomo API 配置只读；要修改连接地址，请编辑 `backend/.env.development` 后重启后端。

## 13. 常见问题

### 13.1 后台能登录，但客户端订阅返回 401

后台登录使用 JWT，客户端订阅使用独立 `subscription_token`。

检查：

- URL 中 `token=` 是否正确。
- 「订阅管理」>「订阅配置」中的 `subscription_token` 是否被修改。
- Docker 环境中的 `SUBSCRIPTION_TOKEN` 是否和你使用的一致。

### 13.2 节点管理为空

检查：

- 是否已有启用订阅。
- 是否点击过「同步节点池」。
- 订阅 URL 是否可访问。
- 宿主机本地开发时是否已叠加 `docker-compose.debug.yml`，并能访问 `http://127.0.0.1:25500/version`。
- `node_filter_patterns` 是否过滤过宽。
- 订阅源是否真的包含可转换节点。
- 订阅同步是否有 `failed` 状态和错误提示。

节点池为空时，访问节点列表或 `/api/sub/mihomo`、`/api/sub/clash`、`/api/sub/v2ray` 会自动尝试同步一次。

### 13.3 Redis 提示 Authentication required 或认证失败

说明 `REDIS_URL` 与实际 Redis 密码不一致。

本地开发如果使用当前 Compose Redis，通常应为：

```env
REDIS_URL=redis://127.0.0.1:6379/0
```

如果你自己的 Redis 设置了密码，例如 `123456`：

```env
REDIS_URL=redis://:123456@127.0.0.1:6379/0
```

修改后重启后端。

### 13.4 无法连接 subconverter

确认服务启动：

```bash
docker compose -f docker-compose.yml -f docker-compose.debug.yml up -d subconverter
```

测试：

```text
http://127.0.0.1:25500/version
```

Docker 完整部署时后端使用 `http://subconverter:25500`，不需要宿主机端口；宿主机开发时需要叠加 `docker-compose.debug.yml` 后使用 `http://127.0.0.1:25500`。

### 13.5 Mihomo 显示 unavailable

宿主机本地开发或已启用调试端口映射时，先测试：

```powershell
Invoke-RestMethod http://127.0.0.1:9090/version
```

如果无法访问，检查 Mihomo 容器是否启动，并确认是否叠加了 `docker-compose.debug.yml`：

```bash
docker compose -f docker-compose.yml -f docker-compose.debug.yml up -d mihomo
```

如果 API 能访问但页面仍不可用，检查：

- Docker 完整部署：`MIHOMO_API_URL=http://mihomo:9090`。
- 宿主机开发：`MIHOMO_API_URL=http://127.0.0.1:9090`。
- 如设置了 `MIHOMO_API_SECRET`，Mihomo 和 Nebula 的密钥必须一致。

### 13.6 智能代理已创建但无法连接

检查：

- 是否点击「重新应用到 Mihomo」。
- 代理状态是否为 `running`。
- Docker 是否映射了智能代理端口范围：默认 `37890-37900`。
- 监听地址是否适合当前场景。容器内 Mihomo 读取 `/root/.config/mihomo/...` 时，后端会把非 `0.0.0.0` 的监听地址绑定为 `0.0.0.0`，方便端口映射。
- 客户端认证是否正确：用户名/密码或 `token:<access_token>`。
- IP 白名单是否把客户端来源 IP 排除。
- 代理候选节点是否为空，流量保护是否排除了全部节点。

### 13.7 Mihomo 热重载失败

检查 runtime 路径是否成对匹配：

```env
MIHOMO_RUNTIME_CONFIG_PATH=/app/data/mihomo-runtime.yaml
MIHOMO_CORE_CONFIG_PATH=/root/.config/mihomo/mihomo-runtime.yaml
```

宿主机开发时：

```env
MIHOMO_RUNTIME_CONFIG_PATH=./data/mihomo-runtime.yaml
MIHOMO_CORE_CONFIG_PATH=/root/.config/mihomo/mihomo-runtime.yaml
```

`MIHOMO_CORE_CONFIG_PATH` 必须是 Mihomo 进程或容器内能看到的路径。

### 13.8 v2rayN 导入后只有一个 Custom

你导入了 Clash/Mihomo 地址。v2rayN 应使用：

```text
/api/sub/v2ray?token=...&group=default
```

### 13.9 流量统计为空

检查：

- 订阅是否启用。
- 订阅源是否返回 `subscription-userinfo` 响应头。
- 是否点击过「刷新流量」。
- 后端日志或日志中心是否有订阅流量刷新错误。

没有流量响应头的订阅不会影响节点使用；智能代理默认也不会排除这类节点，除非开启 `exclude_unknown_traffic`。

### 13.10 请求返回 429

后端有限流中间件，默认每个 IP + 路径分组在 `60` 秒内最多 `180` 次请求。可通过 `.env` 调整：

```env
RATE_LIMIT_REQUESTS=180
RATE_LIMIT_WINDOW_SECONDS=60
```

修改后重启后端。

### 13.11 本地开发时间慢 8 小时

通常是 UTC 时间被当作本地时间展示。请确认 `.env`、`backend/.env.development` 或容器环境里有：

```env
TZ=Asia/Shanghai
```

修改后重启后端；Docker 部署建议执行 `docker compose up -d --build` 重建镜像。旧数据库中已经按 UTC 写入的历史记录不会自动改写，重新同步节点、刷新订阅流量或产生新日志后会按中国时区写入。

### 13.12 WebSocket 状态一直连接不上

内置前端 Nginx 已代理 `/api/ws/status`，正常情况下从管理后台访问同域地址即可连接。排查顺序：

- 确认前端容器已更新到当前版本：`docker compose up -d --build frontend backend`。
- 浏览器里看到的 WebSocket 地址应和管理后台同域，例如 `ws://192.168.31.111:18088/api/ws/status?...`。
- 确认登录 Token 未过期，过期后重新登录。
- 如果前面还有反向代理，需要允许 `Upgrade` 和 `Connection` 头，并把 `/api/` 转发到 Nebula 前端或后端。

### 13.13 保存配置提示 Unknown settings scope

这是旧后端没有识别某些设置作用域时的报错。更新到当前版本后，节点池配置和系统设置都已按作用域返回。若仍然出现：

```bash
git pull --ff-only
docker compose up -d --build backend frontend
docker compose logs --tail=100 backend
```

确认浏览器已刷新到新前端页面，必要时清理浏览器缓存后重新打开。

### 13.14 PWA 无法安装或图标没有更新

先确认访问环境：

- 浏览器需要支持 PWA，并且通常要求 HTTPS 或 `localhost`。
- iOS 需要使用 Safari 的「添加到主屏幕」，第三方浏览器可能不会显示完整安装入口。
- 如果部署在反向代理后面，请确认 `/manifest.webmanifest`、`/sw.js`、`/offline.html` 和 `/icons/` 能被浏览器直接访问，且没有被代理缓存为旧内容。
- 图标更新受浏览器和系统缓存影响较大。升级后可关闭所有 Nebula 页面再重新打开，必要时清理该站点缓存或删除旧主屏应用后重新添加。

### 13.15 蚂蚁节点智能代理为空或无法连接

按顺序检查：

- 蚂蚁代理页是否已经账号登录或上传 `ant.db`，并能看到免费/付费线路节点。
- `ant.db` 登录的数据不会自动刷新；节点变化后需要重新上传文件。
- 账号登录方式可点击「刷新」手动更新，也可在「定时刷新」里开启后台刷新。
- 智能代理新增或编辑时，「数据来源」是否选择了「蚂蚁节点」。
- 指定地区、指定线路/来源、协议类型或指定节点是否过滤过窄。
- 保存智能代理后是否点击「重新应用到 Mihomo」，或全局配置是否开启了按需自动应用。
- Docker Compose 部署时，`.env` 中 `ANT_ADAPTER_BIND_HOST` 是否为 `0.0.0.0`，`ANT_ADAPTER_CONNECT_HOST` 是否为 `backend`；如果外置 Mihomo，则改成 Mihomo 能访问后端容器的地址。
- Docker Compose 部署时，`.env` 中 `MIHOMO_PROXY_SERVER_NAMESERVERS` 建议保持 `127.0.0.11`，否则 Mihomo 可能无法解析 Ant 内部适配器的 `backend` 服务名，表现为策略组测速 `all proxies timeout`、listener 自检 `502`。
- 系统设置里的 `Mihomo API 地址` 是否为 Docker 内网地址 `http://mihomo:9090`，`Mihomo 实际加载路径` 是否为 `/root/.config/mihomo/mihomo-runtime.yaml`。
- 如果智能代理诊断里策略组测速和 listener 自检都正常，但外部设备仍连不上，检查 `.env` 中 `SMART_PROXY_BIND_HOST` 是否仍是 `127.0.0.1`；外部设备访问需要改为 `0.0.0.0`，并重建/重启 `mihomo`。
- 智能代理状态是否为 `running`；如果是 `degraded`，查看诊断弹窗或日志中心中的错误。

蚂蚁代理页不再提供“优先节点”操作。蚂蚁节点最终使用顺序和切换逻辑由智能代理里的候选筛选和调度策略决定。
